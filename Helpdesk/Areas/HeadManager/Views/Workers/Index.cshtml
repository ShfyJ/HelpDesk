@model IEnumerable<ITHelpDesk.Models.Workers>

@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-6">
        <h2 class="text-primary">Сотрудники</h2>
    </div>
    <div class="col-6 text-right">
        <a class="btn btn-primary" asp-area="HeadManager" asp-controller="Requests"><i class="fas fa-list"></i> &nbsp; Заявки</a>
    </div>
</div>

<br />
<div class="p-4 border rounded">
    <table id="tblData" class="table table-striped table-bordered  " style="width:100%">
        <thead class="thead-dark">
            <tr class="table-info">
                <th width="120px">
                    Employee
                </th>
                <th width="200px">
                    Number of works
                </th>

                <th>
                    Status
                </th>
                <th>
                    Tasks on queue
                </th>
                <th>
                    Manager
                </th>
               
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {

                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.User.Fullname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NOfWorks)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.State)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Queue)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Manager.User.Fullname)
                    </td>
                    

                    <td>

                        <div class="text-center" style="width:100px">
                            @*<a asp-action="Edit" asp-route-id="@item.RequestId" class="btn btn-success text-white" style="cursor:pointer">
                                    <i class="fas fa-edit"></i>
                                </a>*@
                            <a asp-action="Details" asp-route-id="@item.WorkerId" class="btn btn-info text-white" style="cursor:pointer">
                                <i class="fas fa-info-circle"></i>
                            </a>
                            @*<a asp-action="Delete" asp-route-id="@item.RequestId" class="btn btn-danger text-white" style="cursor:pointer">
                                    <i class="fas fa-trash-alt"></i>
                                </a>*@
                        </div>
                    </td>
                </tr>

            }

        </tbody>
    </table>

</div>

@*<h1>Сотрудники</h1>

    <p>
        <a asp-area="HeadManager" asp-controller="Requests">Заявки</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Employee
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NOfWorks)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.State)
                </th>
                <th>
                    Score
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Queue)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Manager)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Fullname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NOfWorks)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Score)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Queue)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manager.User.Fullname)
                </td>

                <td>
                    <a asp-action="Details" asp-route-id="@item.WorkerId">Details</a>
                    </td>
            </tr>
    }
        </tbody>
    </table>*@
